class Heart {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
}

class Bomb {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
}
class TurretReload {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
}

class LaserGun {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
}
